<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-4.3.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd
		http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-4.3.xsd">
	
	<aop:config>	<!-- 进行AOP的配置处理 -->
		<!-- 配置切入点表达式，这个表达式直接决定了最终到底在那里去执行代理操作，该操作引入了AspectJ的设计语法 -->
		<aop:pointcut id="myPointcut" expression="execution(public * cn.mldn..service..*.*(..))" />
		<aop:aspect ref="serviceAdvice"><!-- 进行切入处理的操作方法定义，所以要设置好切入的程序类名称 -->
			<aop:before method="handleBefore" pointcut="execution(public * cn.mldn..service..*.*(..)) and args(msg)" arg-names="msg"/>
			<aop:after method="handleAfter" pointcut-ref="myPointcut"/>
			<aop:after-returning method="handleReturn" pointcut-ref="myPointcut" returning="val" arg-names="val"/>
		</aop:aspect>
	</aop:config>
	
	<!-- 定义注解的扫描包，也就是说你必须配置好扫描包之后才可以扫描该包中所有类上出现的注解信息 -->
	<context:component-scan base-package="cn.mldn.mldnspring"/>
</beans>
